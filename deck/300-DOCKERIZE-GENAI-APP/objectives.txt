tout ce que vous avez voulu savoir sur Docker Agentic Compose

Montrer comment Compose interagit de manière transparent pour l'utilisateur avec Docker Model Runner

Le nouveau type 'model' introduit dans la Comopse spec
 - Comment on définit des modèles
 - Quels sont les paramètres que l'on peut définir
   - model: l'OCI artifact contenant le model
   - context_size: le nombre maximal de token utilisé pour le context du model
   - runtime_flags: temperature `--temp`, top-k & top-p `--top-k`, raisonnement `--reasoning-budget`, nb `--threads`, `--verbose`, `--verbose-prompt`, `--logs-x` options
 - Comment associer un service avec un ou des modèles:
   - short syntax
   - long syntax